const twitchIrcParser = (ircMessage) => {
    //TODO Parse the difference parts of messages as well as the types of
    //messages
    let tagPortion = ircMessage

}

module.exports = twitchIrcParser;
